# path_following

```
cd ~/robot_ws/src
git clone https://github.com/pby04188/path_following.git
cd ..
colcon build --symlink-install --packages-select path_following
source ~/.bashrc
ros2 launch path_following run_PathFollowing.launch.py 
```
